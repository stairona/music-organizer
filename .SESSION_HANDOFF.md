# SESSION_HANDOFF.md — Music Organizer v2
> v2-cli-rebuild branch — CDJ-safe done, 3 prompts remaining

**Handoff refreshed: 2026-03-13** — Branch `v2-cli-rebuild`; document below remains relevant.

---

## 1. Quick Context

**Project**: `music-organizer` — DJ-grade CLI tool for organizing music libraries by genre
**Location**: `/Users/nicolasaguirre/Development/light-projects/music-organizer`
**Branch**: `v2-cli-rebuild` (NOT yet merged to main)
**Remote**: `github.com/stairona/music-organizer`
**Python**: 3.13.5 via Anaconda (`/opt/anaconda3/bin/python`)
**Env**: MacBook Air M4, 16GB, macOS
**Tests**: 67 passing via `pytest`
**Version**: 2.0.0

---

## 2. Current State

### What's Done (v2-cli-rebuild)
- **Subcommand CLI**: `analyze`, `organize`, `genres`, `undo`, `config`
- **Interactive DJ workflow**: guided prompts for source/dest/mode/level/profile
- **Journal/undo system**: JSON journal at `~/.config/music-organizer/journal.json`
- **Config system**: `~/.config/music-organizer/config.json` with init/show/path
- **CDJ-safe profile**: filename sanitization, depth limits, path length warnings, folder count warnings
- **Genre classification**: 80+ specific genres across 10 general buckets, word-boundary regex
- **67 tests** all passing

### Git Log
```
a995d9c (HEAD -> v2-cli-rebuild) feat: CDJ-safe profile — filename sanitization, depth limits, path/folder warnings
1906344 feat: v2.0 — subcommand CLI, journal/undo, interactive mode, config system
4020c10 (origin/main, main) fix: word-boundary regex, remove ambiguous keywords, use Counter
b8bc5fa chore: hide CLAUDE.md
dcc8631 feat: Phase 2 - documentation and changelog
```

### Commands
```
music-organizer analyze <source> [--level] [--report CSV]
music-organizer organize <source> <dest> [--mode copy|move] [--level] [--dry-run] [--interactive] [--profile cdj-safe]
music-organizer genres [--bucket NAME]
music-organizer undo
music-organizer config [init|show|path]
```

---

## 3. What's Left — Prompts 2, 3, 4

Full prompts are saved in **CLAUDE_CODE_PROMPTS.md** (same directory).

| # | Feature | Status |
|---|---------|--------|
| 1 | CDJ-safe profile | Done (commit a995d9c) |
| 2 | Custom genre mapping from config | Not started |
| 3 | DJ presets (club/latin/open-format/festival) | Not started |
| 4 | Merge v2-cli-rebuild to main, push | Not started |

**To run a prompt**: Open Claude Code in terminal, paste the prompt from `CLAUDE_CODE_PROMPTS.md`.

---

## 4. Package Structure

```
src/music_organizer/
├── __init__.py
├── __main__.py          # python -m music_organizer
├── cli.py               # subcommand parser + dispatch
├── classify.py          # genre inference (metadata → path → unknown)
├── fileops.py           # copy/move, collision handling, CDJ-safe sanitize
├── journal.py           # undo journal system
├── main.py              # legacy entry point (backward compat)
├── reporting.py         # CSV + summary output
├── rules.py             # 80+ genres, GENERAL_MAP, PATH_KEYWORDS
├── scanner.py           # file discovery
├── tags.py              # mutagen metadata reading
└── commands/
    ├── __init__.py
    ├── analyze.py       # scan+classify without file moves
    ├── config.py        # init/show/path config management
    ├── genres.py        # display genre list by bucket
    ├── interactive.py   # guided DJ workflow prompts
    ├── organize.py      # copy/move with journal + CDJ-safe
    └── undo.py          # interactive undo with confirmation
```

### Tests (67 total)
```
tests/
├── test_classify.py     (25 tests)
├── test_cli.py          (12 tests)
├── test_fileops.py      (16 tests)
├── test_journal.py      (5 tests)
├── test_organize.py     (3 tests)
└── test_scanner.py      (6 tests)
```

---

## 5. Resume Commands

```bash
cd /Users/nicolasaguirre/Development/light-projects/music-organizer
git status                     # should be clean on v2-cli-rebuild
python -m pytest tests/ -q     # should see 67 passed
cat CLAUDE_CODE_PROMPTS.md     # see next prompts to run
```

---

## 6. Key Files

| File | Purpose |
|------|---------|
| CLAUDE.md | Project rules for AI assistants (untracked, local only) |
| CLAUDE_CODE_PROMPTS.md | Prompts 1-4 for Claude Code (new) |
| SESSION_HANDOFF.md | This file |
| pyproject.toml | Package config, entry point: music-organizer |
| requirements.txt | mutagen>=1.47.0 |

---

## 7. Workflow: VS Code (Copilot) + Claude Code (Terminal)

- **VS Code Copilot**: Architecture decisions, code review, multi-file edits, this handoff
- **Claude Code**: Execute prompts 2-4 sequentially in terminal
- After each prompt: verify tests pass (`pytest`), then commit
- After prompt 4: branch merged to main, pushed to GitHub

---

## 8. Development Workspace Recovery & Organization (March 12, 2026)

This section documents a separate session where the Development workspace was recovered and reorganized after Claude Code accidentally deleted project files.

### What Happened
- Another Claude Code session deleted the `music-organizer` project folder entirely.
- The parent `~/Development` repo (token-tracker) had accidentally staged `light-projects/music-organizer` and `light-projects/bird-simulation` as gitlinks (no `.gitmodules` defined).
- The project was recovered by cloning from GitHub: `github.com/stairona/music-organizer`.

### Recovery Steps Taken
1. Confirmed `~/Development/light-projects/music-organizer` was missing from disk.
2. Found that `~/Development/.git` still referenced the project as a gitlink at commit `f6d8fe6`.
3. Re-cloned: `git clone https://github.com/stairona/music-organizer.git ~/Development/light-projects/music-organizer`
4. Removed accidental gitlink staging from parent repo: `git rm --cached -rf light-projects/bird-simulation light-projects/music-organizer`
5. Added `light-projects/` to `~/Development/.gitignore` so nested repos are never staged again.

### Workspace Reorganization
| Action | Detail |
|---|---|
| Moved | `~/Development/token_tracker.py` → `~/Development/light-projects/token-tracker/token_tracker.py` |
| Moved | `~/Development/README.md` → `~/Development/light-projects/token-tracker/README.md` |
| Updated | `~/.zshrc` — `claude()` function and `tokenbar` alias now point to new token-tracker path |
| Created | `~/Development/DEVELOPMENT_GUIDE.md` — master workspace reference |
| Created | `~/Development/AI_TOOLS_RULES.md` — per-tool permission and safety rules |
| Created | `~/Development/CLAUDE.md` — Development-level Claude safety policy |
| Updated | `~/Development/light-projects/CLAUDE.md` — full hard rules, context window guidance, handoff reminder |
| Created | `~/.claude/CLAUDE.md` — global machine-wide safety policy |
| Created | `~/.claude/projects/CLAUDE.md` — session-scope safety policy |

### Final Workspace Structure
```
~/Development/
├── CLAUDE.md                  # Development-level Claude safety policy
├── DEVELOPMENT_GUIDE.md       # Master workspace reference
├── AI_TOOLS_RULES.md          # Per-tool permissions (Claude RESTRICTED, VS Code/Cursor FULL)
├── PROJECTS_ORGANIZATION.md   # Layout conventions and maintenance checklist
├── light-projects/
│   ├── CLAUDE.md              # Auto-loaded by Claude Code — hard rules, workflow, env
│   ├── music-organizer/       # Python CLI music library organizer (this repo)
│   ├── bird-simulation/       # Bird collision and mortality simulation toolkit
│   └── token-tracker/
│       ├── token_tracker.py   # macOS menu bar Claude token usage tracker
│       └── README.md
└── random/                    # Scratch folder (empty except .DS_Store)
```

### zshrc State (verified clean)
```zsh
function claude() {
    pkill -f token_tracker.py 2>/dev/null
    /opt/homebrew/bin/python3.11 /Users/nicolasaguirre/Development/light-projects/token-tracker/token_tracker.py &
    command claude "$@"
    pkill -f token_tracker.py 2>/dev/null
}
alias tokenbar="/opt/homebrew/bin/python3.11 /Users/nicolasaguirre/Development/light-projects/token-tracker/token_tracker.py"
alias claude='claude --model stepfun/step-3.5-flash:free'
```

### Safety Policies Now Active
All Claude Code sessions in `~/Development/` and below inherit:
- No `rm`, `rm -rf`, `rmdir`, or any destructive command.
- No file deletion without explicit user confirmation.
- No overwriting files without showing a diff first.
- End of session: Moved/Renamed Files summary + Deletion Candidates list.

### Moved/Renamed Files (this session)
| Old Path | New Path | Reason |
|---|---|---|
| `~/Development/token_tracker.py` | `~/Development/light-projects/token-tracker/token_tracker.py` | Consolidate token-tracker assets into project folder |
| `~/Development/README.md` | `~/Development/light-projects/token-tracker/README.md` | Keep documentation with its project |

### Deletion Candidates
None. No files deleted.
